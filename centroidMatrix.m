%This function will create a centroid matrix with 3 centroids
function centroid = centroidMatrix(centroid_one, centroid_two, centroid_three)
    centroid = [centroid_one, centroid_two, centroid_three];
end
